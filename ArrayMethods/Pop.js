//Making Array
let arr = ["Rohaan", "Nadeem"];

//pop
console.log(arr.pop()); //this will delete the last indexed value from the array
//Nadeem   (removed)
console.log(arr);
//["Rohaan"]

console.log(arr.pop()); //this will delete the last indexed value from the array
//Rohaan   (removed)
console.log(arr);
//[]

console.log(arr.pop()); //this will delete the last indexed value from the array
//undefined
console.log(arr);
//[]